# إعداد النشر على Laravel Cloud

## 📁 بنية المستودع

المشروع موجود في المجلد الفرعي `memoria-app/`. تم نسخ ملفات `composer.json` و `composer.lock` إلى جذر المستودع لضمان أن Laravel Cloud يجدها.

## ⚙️ إعداد Laravel Cloud

عند إعداد المشروع في Laravel Cloud:

1. **Root Directory**: في إعدادات المشروع (Settings → General)، حدد:
   ```
   memoria-app
   ```

2. **ملفات النشر**: تم إنشاء `laravel.json` في جذر المستودع والذي يحتوي على الأوامر المطلوبة للنشر.

## 🔄 بعد التحديثات

بعد كل تحديث للمشروع، تأكد من:
1. رفع التغييرات إلى Git
2. Laravel Cloud سينشر تلقائياً عند الـ push

## 📝 ملاحظات

- ملفات `composer.json` و `composer.lock` موجودة في كلا المكانين (الجذر و `memoria-app/`)
- ملف `laravel.json` في الجذر يحتوي على الأوامر التي تعمل من داخل `memoria-app/`
- تأكد من تحديد **Root Directory** في إعدادات Laravel Cloud

