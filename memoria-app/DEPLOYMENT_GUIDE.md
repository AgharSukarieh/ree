# دليل نشر المشروع على Laravel Cloud

## 📋 المتطلبات الأساسية

1. حساب على [Laravel Cloud](https://cloud.laravel.com)
2. المشروع موجود على Git (GitHub, GitLab, أو Bitbucket)
3. قاعدة بيانات MySQL (يمكن إنشاؤها من لوحة تحكم Laravel Cloud)

## 🚀 خطوات النشر

### 1. التأكد من إعدادات Git

تأكد من أن المشروع موجود على مستودع Git:

```bash
git status
git add .
git commit -m "Prepare for Laravel Cloud deployment"
git push origin main
```

### 2. إنشاء حساب على Laravel Cloud

1. اذهب إلى [Laravel Cloud](https://cloud.laravel.com)
2. سجل دخول بحسابك
3. إذا لم يكن لديك حساب، أنشئ واحداً

### 3. ربط المشروع بـ Laravel Cloud

1. من لوحة التحكم، اضغط على **"New Project"**
2. اختر **"Import from Git"**
3. اختر المستودع الخاص بك (GitHub/GitLab/Bitbucket)
4. اختر الفرع (Branch) - عادة `main` أو `master`
5. اختر اسم للمشروع
6. **مهم جداً**: في إعدادات المشروع، اذهب إلى **"Settings"** → **"General"**
7. في حقل **"Root Directory"**، أدخل: `memoria-app`
8. احفظ الإعدادات

### 4. إعداد قاعدة البيانات

1. من لوحة تحكم المشروع، اذهب إلى **"Databases"**
2. اضغط على **"Create Database"**
3. اختر نوع قاعدة البيانات (MySQL)
4. سجل بيانات الاتصال:
   - `DB_HOST`
   - `DB_PORT`
   - `DB_DATABASE`
   - `DB_USERNAME`
   - `DB_PASSWORD`

### 5. إعداد متغيرات البيئة (Environment Variables)

1. من لوحة تحكم المشروع، اذهب إلى **"Environment Variables"**
2. أضف المتغيرات التالية:

#### المتغيرات الأساسية:
```
APP_NAME=Memoria
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-app-name.laravel.cloud
```

#### قاعدة البيانات (من الخطوة السابقة):
```
DB_CONNECTION=mysql
DB_HOST=your-db-host
DB_PORT=3306
DB_DATABASE=your-database-name
DB_USERNAME=your-username
DB_PASSWORD=your-password
```

#### Cloudinary:
```
CLOUDINARY_URL=cloudinary://629249255372626:DQATNuq02hWbcKW33DY_xRlnQTI@dozvsu2rp
```

أو المتغيرات المنفصلة:
```
CLOUDINARY_CLOUD_NAME=dozvsu2rp
CLOUDINARY_API_KEY=629249255372626
CLOUDINARY_API_SECRET=DQATNuq02hWbcKW33DY_xRlnQTI
```

#### مفتاح التطبيق (APP_KEY):
```
APP_KEY=base64:your-generated-key-here
```

**ملاحظة:** يمكنك توليد المفتاح محلياً باستخدام:
```bash
php artisan key:generate --show
```

### 6. تشغيل Migrations

بعد إعداد قاعدة البيانات والمتغيرات:

1. من لوحة التحكم، اذهب إلى **"Deployments"**
2. اضغط على **"Deploy Now"** أو انتظر النشر التلقائي
3. بعد النشر، اذهب إلى **"SSH"** أو **"Terminal"**
4. شغل الأمر:
```bash
php artisan migrate --force
```

أو يمكنك إضافة الأمر تلقائياً في ملف `laravel.json` (تم إضافته بالفعل)

### 7. ربط Storage

لربط مجلد التخزين العام:

1. من Terminal في Laravel Cloud:
```bash
php artisan storage:link
```

### 8. التحقق من النشر

1. بعد اكتمال النشر، ستجد رابط التطبيق في لوحة التحكم
2. افتح الرابط في المتصفح للتحقق من أن كل شيء يعمل

## 📝 ملف laravel.json

تم إنشاء ملف `laravel.json` في المشروع والذي يحتوي على:

- **Build Commands**: تثبيت npm dependencies وبناء الأصول
- **Deploy Commands**: تشغيل migrations ومسح الكاش

## 🔧 إعدادات إضافية

### تشغيل Seeders (اختياري)

إذا كنت تريد تشغيل Seeders:

1. من Terminal في Laravel Cloud:
```bash
php artisan db:seed --force
```

### مسح الكاش

إذا واجهت مشاكل في الكاش:

```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

### إعادة بناء الكاش

```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

## ⚠️ ملاحظات مهمة

1. **لا ترفع ملف `.env`** - تأكد من أنه موجود في `.gitignore`
2. **استخدم Environment Variables** في Laravel Cloud لإعدادات الإنتاج
3. **APP_DEBUG يجب أن يكون `false`** في الإنتاج
4. **APP_URL** يجب أن يكون الرابط الصحيح للتطبيق على Laravel Cloud
5. **قاعدة البيانات** - تأكد من أن قاعدة البيانات جاهزة قبل النشر
6. **Root Directory**: إذا كان المشروع في مجلد فرعي (مثل `memoria-app`)، يجب تحديد **Root Directory** في إعدادات المشروع في Laravel Cloud:
   - اذهب إلى **Settings** → **General**
   - في حقل **Root Directory**، أدخل: `memoria-app`
   - احفظ الإعدادات
7. **ملفات composer.json و composer.lock**: تم نسخها إلى جذر المستودع لضمان أن Laravel Cloud يجدها

## 🐛 حل المشاكل الشائعة

### المشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل:** تحقق من متغيرات البيئة `DB_*` في Laravel Cloud

### المشكلة: خطأ 500
**الحل:** 
1. تحقق من السجلات (Logs) في Laravel Cloud
2. تأكد من أن `APP_KEY` موجود
3. تأكد من أن `APP_DEBUG=false` في الإنتاج

### المشكلة: الصور لا تظهر
**الحل:** 
1. تأكد من إعدادات Cloudinary
2. شغل `php artisan storage:link`
3. تحقق من `FILESYSTEM_DISK` في متغيرات البيئة

### المشكلة: خطأ 403 Forbidden
**الحل:** 
1. **تحقق من Root Directory**: تأكد من أن Root Directory مضبوط على `memoria-app` في Settings → General
2. **تحقق من الصلاحيات**: تأكد من أن مجلدات `storage` و `bootstrap/cache` لها صلاحيات الكتابة (775)
3. **تحقق من APP_KEY**: تأكد من وجود `APP_KEY` في متغيرات البيئة
4. **تحقق من APP_URL**: تأكد من أن `APP_URL` مطابق لرابط التطبيق على Laravel Cloud
5. **تحقق من السجلات**: اذهب إلى Logs في Laravel Cloud لرؤية الخطأ التفصيلي
6. **شغل الأوامر التالية في Terminal**:
   ```bash
   cd memoria-app
   chmod -R 775 storage bootstrap/cache
   php artisan storage:link
   php artisan config:clear
   php artisan cache:clear
   ```

## 📚 روابط مفيدة

- [Laravel Cloud Documentation](https://docs.cloud.laravel.com)
- [Laravel Deployment Guide](https://laravel.com/docs/deployment)

## ✅ قائمة التحقق قبل النشر

- [ ] المشروع موجود على Git
- [ ] ملف `laravel.json` موجود
- [ ] قاعدة البيانات تم إنشاؤها
- [ ] جميع متغيرات البيئة تم إضافتها
- [ ] `APP_KEY` تم توليده وإضافته
- [ ] `APP_DEBUG=false` في الإنتاج
- [ ] `APP_URL` صحيح
- [ ] إعدادات Cloudinary موجودة
- [ ] Migrations جاهزة للتشغيل

---

**بعد اكتمال النشر، سيتم تحديث التطبيق تلقائياً عند كل push إلى الفرع الرئيسي!**

