# 🗄️ إنشاء قاعدة بيانات منفصلة لمشروع Memoria

## ⚠️ المشكلة الحالية

قاعدة البيانات المشتركة تحتوي على جدول `users` ببنية مختلفة (بدون عمود `qr_id`)، بينما مشروع memoria يحتاج جدول `users` بعمود `qr_id` كـ primary key.

## ✅ الحل: إنشاء قاعدة بيانات منفصلة

### الخطوة 1: إنشاء قاعدة بيانات جديدة في Laravel Cloud

1. **اذهب إلى Laravel Cloud Dashboard**
   - افتح: https://cloud.laravel.com
   - اختر مشروعك: **memoria**

2. **إنشاء قاعدة بيانات جديدة**
   - من القائمة الجانبية: **Databases**
   - اضغط **Create Database** أو **+ New Database**
   - اختر:
     - **Type**: MySQL 8
     - **Name**: `memoria_db` (أو أي اسم تريده)
     - **Region**: اختر نفس المنطقة (US East 2)
   - اضغط **Create**

3. **انتظر حتى يتم إنشاء قاعدة البيانات**
   - قد يستغرق بضع دقائق

### الخطوة 2: الحصول على بيانات الاتصال

بعد إنشاء قاعدة البيانات:

1. **افتح قاعدة البيانات الجديدة**
2. **اذهب إلى قسم Credentials**
3. **انسخ البيانات التالية:**
   - `DB_HOST` - عنوان الخادم
   - `DB_PORT` - عادة `3306`
   - `DB_DATABASE` - اسم قاعدة البيانات
   - `DB_USERNAME` - اسم المستخدم
   - `DB_PASSWORD` - كلمة المرور

### الخطوة 3: تحديث متغيرات البيئة

1. **اذهب إلى Environment Variables**
   - من القائمة الجانبية: **Settings** → **Environment Variables**

2. **حدّث متغيرات قاعدة البيانات:**
   - احذف أو غيّر القيم القديمة:
     ```
     DB_HOST=db-a0c6457a-9359-413c-afeb-404f9298d329.us-east-2.db.laravel.cloud
     DB_DATABASE=main
     DB_USERNAME=say3nmqlscsxcuyi
     DB_PASSWORD=FbDJTghQY5FcZuk6pfNh
     ```
   
   - أضف القيم الجديدة من قاعدة البيانات الجديدة:
     ```
     DB_HOST=[القيمة الجديدة من قاعدة البيانات الجديدة]
     DB_PORT=3306
     DB_DATABASE=[اسم قاعدة البيانات الجديدة]
     DB_USERNAME=[اسم المستخدم الجديد]
     DB_PASSWORD=[كلمة المرور الجديدة]
     ```

3. **احفظ التغييرات**

### الخطوة 4: إعادة النشر

1. **اذهب إلى Deployments**
2. **اضغط Redeploy** أو **Deploy Now**
3. **انتظر حتى يكتمل النشر**

## 📋 مثال على المتغيرات المطلوبة

بعد إنشاء قاعدة البيانات الجديدة، ستكون المتغيرات كالتالي:

```env
APP_NAME="Memoria"
APP_ENV=production
APP_DEBUG=false
APP_URL="https://memoria-master-ihn1qf.laravel.cloud"
LOG_CHANNEL=laravel-cloud-socket
LOG_STDERR_FORMATTER=Monolog\Formatter\JsonFormatter

DB_CONNECTION=mysql
DB_HOST=[من قاعدة البيانات الجديدة]
DB_PORT=3306
DB_DATABASE=[من قاعدة البيانات الجديدة]
DB_USERNAME=[من قاعدة البيانات الجديدة]
DB_PASSWORD=[من قاعدة البيانات الجديدة]

SESSION_DRIVER=cookie
CACHE_STORE=database
SCHEDULE_CACHE_DRIVER=database
VITE_APP_NAME="${APP_NAME}"
```

## ✅ التحقق من النجاح

بعد إعادة النشر، تحقق من:

1. **النشر نجح بدون أخطاء**
2. **الجداول تم إنشاؤها بنجاح:**
   - اذهب إلى قاعدة البيانات الجديدة
   - تحقق من وجود جدول `users` بعمود `qr_id`
   - تحقق من وجود جميع الجداول الأخرى

## 🔗 روابط مفيدة

- Laravel Cloud Dashboard: https://cloud.laravel.com
- تطبيق Memoria: https://memoria-master-ihn1qf.laravel.cloud

## ⚠️ ملاحظات مهمة

- **قاعدة البيانات الجديدة ستكون فارغة** - سيتم إنشاء جميع الجداول من migrations
- **لا تشارك قاعدة البيانات** مع مشاريع أخرى لتجنب تعارض البنية
- **احفظ بيانات الاتصال** في مكان آمن

